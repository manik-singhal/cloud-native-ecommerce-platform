# DNS & External Access

The application is accessible externally through a custom domain configured with Route53.

## Why Route53

Route53 is a managed DNS service that integrates directly with AWS. It handles domain resolution without requiring manual DNS server setup.

## Validation

The application is accessible via the custom domain.

![Domain Access](../../screenshots/foundations/domain-working.png)
